# Chat global de la barra — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un chat global para que los jugadores del prode se manden mensajes en vivo durante el Mundial, con moderación de admin y costo de Firestore acotado (últimos 50, conectado sólo al abrir la pestaña).

**Architecture:** Lógica pura de validación en `lib/chat.js` (testeada). Colección `messages` en Firestore con reglas que sólo dejan escribir tu propio mensaje y sólo si tenés doc de jugador (bloquea expulsados). `firebase-service.js` gana `subscribeMessages`/`sendMessage`/`deleteMessage`. Nueva pantalla `Chat.jsx` que se suscribe sólo mientras está montada. La pestaña "Grupos" (placeholder) se reemplaza por "Chat".

**Tech Stack:** React (Babel standalone, sin build), Firebase Firestore compat, `node --test`.

**Spec:** `docs/superpowers/specs/2026-06-14-chat-design.md`

**Contexto para quien implementa:**
- App estática sin bundler: cada archivo se carga con `<script>` en `index.html`; los `lib/` usan IIFE dual navegador/Node (referencia `lib/weekly.js`); las pantallas se cargan con `<script type="text/babel">` y se asignan a `window`. Tests: `node --test` desde la raíz; **la suite actual tiene 66 tests verdes**.
- `firebase-service.js` (IIFE) expone `window.ProdeDB`. Helpers internos ya existentes a reutilizar: `collection(name)`, `firestoreNow()` (devuelve `serverTimestamp()`), `currentPlayerId()`, `isAdmin()`, `state.player` (tiene `name`, `avatarTone`), `state.ready`, `state.db`. Mirá `subscribeRanking`/`subscribeSpecialResults` como molde de suscripciones y `expelPlayer` como molde de borrados.
- Componentes globales (de `components.jsx`): `Avatar`, `AppBar`, `Eyebrow`, `Btn`, `Pill`. Hooks: `useState`, `useEffect`, `useRef` (const globales desde React).
- JSX se valida con `npx --yes esbuild <archivo> --loader:.jsx=jsx --outfile=NUL --log-level=error`.
- Comentarios en español. Sin datos demo: estados vacíos honestos.

---

### Task 1: `lib/chat.js` — validación de mensajes (TDD)

**Files:**
- Create: `lib/chat.js`
- Create: `lib/chat.test.js`

- [ ] **Step 1: Write the failing tests** — crear `lib/chat.test.js`:

```js
const test = require("node:test");
const assert = require("node:assert");
const { validMessage, MAX_LEN } = require("./chat.js");

test("MAX_LEN es 500", () => {
  assert.strictEqual(MAX_LEN, 500);
});

test("validMessage: texto normal → ok con trim", () => {
  assert.deepStrictEqual(validMessage("  hola barra  "), { ok: true, text: "hola barra" });
});

test("validMessage: vacío o sólo espacios → no ok", () => {
  assert.deepStrictEqual(validMessage(""), { ok: false });
  assert.deepStrictEqual(validMessage("   "), { ok: false });
  assert.deepStrictEqual(validMessage(null), { ok: false });
  assert.deepStrictEqual(validMessage(undefined), { ok: false });
});

test("validMessage: no-string → no ok", () => {
  assert.deepStrictEqual(validMessage(42), { ok: false });
  assert.deepStrictEqual(validMessage({}), { ok: false });
});

test("validMessage: exactamente 500 pasa; 501 se recorta a 500", () => {
  const exact = "a".repeat(500);
  assert.deepStrictEqual(validMessage(exact), { ok: true, text: exact });
  const over = "b".repeat(600);
  const r = validMessage(over);
  assert.strictEqual(r.ok, true);
  assert.strictEqual(r.text.length, 500);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test lib/chat.test.js`
Expected: FAIL — `Cannot find module './chat.js'`

- [ ] **Step 3: Write minimal implementation** — crear `lib/chat.js`:

```js
/* ============================================================
   PRODE REFUGIO — Chat (lógica pura).
   Validación/saneo del texto de un mensaje. Dual navegador/Node.
   ============================================================ */
(function (global) {
  const MAX_LEN = 500;

  // Devuelve { ok:false } si el texto no sirve (vacío, sólo espacios, no-string);
  // { ok:true, text } con el texto saneado (trim + recorte a MAX_LEN) si sirve.
  function validMessage(text) {
    if (typeof text !== "string") return { ok: false };
    const t = text.trim();
    if (t.length === 0) return { ok: false };
    return { ok: true, text: t.slice(0, MAX_LEN) };
  }

  const api = { validMessage, MAX_LEN };
  global.ProdeChat = api;
  if (typeof module !== "undefined" && module.exports) module.exports = api;
})(typeof window !== "undefined" ? window : globalThis);
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test lib/chat.test.js` — Expected: PASS (5 tests)

- [ ] **Step 5: Run full suite**

Run: `node --test` — Expected: 71/71 (66 previos + 5)

- [ ] **Step 6: Commit**

```bash
git add lib/chat.js lib/chat.test.js
git commit -m "lib/chat: validMessage (trim, no vacío, recorte a 500)"
```

---

### Task 2: capa de datos en `firebase-service.js` + script en `index.html`

**Files:**
- Modify: `firebase-service.js` (3 funciones nuevas + exports)
- Modify: `index.html` (script `lib/chat.js` después de `lib/specials.js`)

Sin lógica pura nueva (reusa `ProdeChat.validMessage`); verificación = `node --check` + suite.

- [ ] **Step 1: `index.html`** — después de `<script src="lib/specials.js"></script>` agregar:

```html
<script src="lib/specials.js"></script>
<script src="lib/chat.js"></script>
```

(Antes de `firebase-service.js`. Verificá el orden: ...ranking.js, weekly.js, specials.js, chat.js, firebase-service.js...)

- [ ] **Step 2: `firebase-service.js`** — agregar las tres funciones después de `touchPresence` y antes de `subscribePlayersPrivate`:

```js
  // Chat global en vivo: últimos `max` mensajes (orden cronológico). Devuelve unsub.
  function subscribeMessages(cb, max = 50) {
    if (!state.ready) { cb([]); return () => {}; }
    return collection("messages").orderBy("createdAt", "desc").limit(max).onSnapshot((snap) => {
      const list = snap.docs.map((doc) => ({ id: doc.id, ...doc.data() }));
      list.reverse(); // viejo → nuevo para la UI
      cb(list);
    }, (e) => console.error("[Prode Refugio] messages snapshot", e));
  }

  // Envía un mensaje del jugador actual al chat. Valida y sanea el texto.
  async function sendMessage(text) {
    const id = currentPlayerId();
    if (!state.ready || !id) return { ok: false };
    const v = window.ProdeChat ? window.ProdeChat.validMessage(text) : { ok: false };
    if (!v.ok) return { ok: false };
    const p = state.player || {};
    await collection("messages").add({
      uid: id,
      name: p.name || "Jugador",
      avatarTone: p.avatarTone || "olive",
      text: v.text,
      createdAt: firestoreNow(),
    });
    return { ok: true };
  }

  // Borra un mensaje (moderación). Requiere ser admin (lo exigen las reglas).
  async function deleteMessage(messageId) {
    if (!state.ready || !messageId) return;
    await collection("messages").doc(messageId).delete();
  }
```

Y en el export `window.ProdeDB = { ... }` agregar:

```js
    subscribeMessages,
    sendMessage,
    deleteMessage,
```

**Nota:** `collection("messages").add(...)` usa el método `add` del SDK compat (genera id automático). `collection()` devuelve `state.db.collection(name)`, que tiene `.add`. Correcto.

- [ ] **Step 3: Verificar**

Run: `node --check firebase-service.js && node --test`
Expected: sin errores de sintaxis; 71/71.

- [ ] **Step 4: Commit**

```bash
git add firebase-service.js index.html
git commit -m "firebase-service: subscribeMessages / sendMessage / deleteMessage (chat)"
```

---

### Task 3: reglas de Firestore para `messages`

**Files:**
- Modify: `firestore.rules` (nuevo bloque antes del comentario final de cierre)

- [ ] **Step 1: Agregar el bloque** — en `firestore.rules`, justo después del bloque `match /meta/{doc} { ... }` y antes del comentario `// Todo lo no declarado ...`:

```
    // ---- messages: chat global de la barra ----
    // Sólo escribís TU propio mensaje, con texto válido, y SÓLO si tenés doc de
    // jugador (expulsado = sin players/{uid} → no puede escribir). Admin borra.
    match /messages/{id} {
      allow read: if isSignedIn();
      allow create: if isSignedIn()
        && request.resource.data.uid == request.auth.uid
        && exists(/databases/$(database)/documents/players/$(request.auth.uid))
        && request.resource.data.text is string
        && request.resource.data.text.size() > 0
        && request.resource.data.text.size() <= 500
        && request.resource.data.keys().hasOnly(['uid','name','avatarTone','text','createdAt']);
      allow update: if false;
      allow delete: if isAdmin();
    }
```

- [ ] **Step 2: Verificación** — sin test automatizado de reglas en este repo. Releer el bloque y confirmar: `read` para logueados; `create` exige uid propio + `exists(players/{uid})` + texto 1..500 + sólo esas 5 claves; `update` denegado; `delete` sólo admin. Confirmar que `isSignedIn()`/`isAdmin()` ya existen como funciones arriba en el archivo.

- [ ] **Step 3: Commit**

```bash
git add firestore.rules
git commit -m "Reglas: messages (chat) — uid propio + doc de jugador + admin borra"
```

**Nota para el reporte final:** el usuario debe **republicar `firestore.rules`** en la consola de Firebase para que el chat funcione (hoy `messages` está denegado por defecto).

---

### Task 4: pantalla `Chat.jsx`

**Files:**
- Create: `screens/Chat.jsx`
- Modify: `index.html` (cargar `screens/Chat.jsx` junto a las otras pantallas)

- [ ] **Step 1: Crear `screens/Chat.jsx`**:

```jsx
/* ============================================================
   SCREEN: Chat global de la barra
   Se suscribe SÓLO mientras está montada (al entrar a la pestaña)
   y se desuscribe al salir → lecturas de Firestore acotadas.
   ============================================================ */

function Chat({ go }) {
  const [messages, setMessages] = useState(null); // null = cargando
  const [text, setText] = useState("");
  const [sending, setSending] = useState(false);
  const listRef = useRef(null);
  const lastSentRef = useRef(0);

  const myUid = window.ProdeDB?.getUser?.()?.uid;
  const isAdmin = !!window.ProdeDB?.isAdmin?.();

  useEffect(() => {
    const unsub = window.ProdeDB?.subscribeMessages?.((list) => setMessages(list));
    return () => unsub && unsub();
  }, []);

  // Auto-scroll al último mensaje cuando llegan nuevos.
  useEffect(() => {
    const el = listRef.current;
    if (el) el.scrollTop = el.scrollHeight;
  }, [messages]);

  const send = async () => {
    const v = window.ProdeChat?.validMessage?.(text);
    if (!v || !v.ok || sending) return;
    if (Date.now() - lastSentRef.current < 1000) return; // freno anti-spam (1s)
    setSending(true);
    try {
      const r = await window.ProdeDB?.sendMessage?.(text);
      if (r && r.ok) { setText(""); lastSentRef.current = Date.now(); }
    } catch (e) {
      console.warn("[Prode Refugio] enviar mensaje", e);
    } finally {
      setSending(false);
    }
  };

  const onKey = (e) => { if (e.key === "Enter" && !e.shiftKey) { e.preventDefault(); send(); } };

  const del = async (id) => {
    try { await window.ProdeDB?.deleteMessage?.(id); }
    catch (e) { console.error("[Prode Refugio] borrar mensaje", e); }
  };

  const hora = (createdAt) => {
    const ms = createdAt?.toMillis ? createdAt.toMillis() : (createdAt ? Date.parse(createdAt) : 0);
    if (!ms) return "";
    const d = new Date(ms);
    return `${String(d.getHours()).padStart(2,"0")}:${String(d.getMinutes()).padStart(2,"0")}`;
  };

  const loading = messages === null;
  const list = messages || [];

  return (
    <div style={{display:"flex", flexDirection:"column", height:"100%", minHeight:"100vh"}}>
      <AppBar title="Chat" subtitle="La barra del Refugio"/>

      {/* lista de mensajes */}
      <div ref={listRef} style={{flex:1, overflowY:"auto", padding:"8px 14px 12px"}}>
        {loading && (
          <div style={{padding:"30px 16px", textAlign:"center", fontSize:13, color:"var(--char-300)"}}>
            Cargando chat...
          </div>
        )}
        {!loading && list.length === 0 && (
          <div style={{padding:"40px 16px", textAlign:"center", fontSize:13, color:"var(--char-300)", lineHeight:1.5}}>
            Todavía no hay mensajes. <span style={{color:"var(--neon-citrus)"}}>Rompé el hielo.</span>
          </div>
        )}
        {list.map((m) => {
          const mine = m.uid === myUid;
          return (
            <div key={m.id} style={{
              display:"flex", gap:10, marginBottom:12,
              flexDirection: mine ? "row-reverse" : "row",
            }}>
              <Avatar initials={window.ProdeRanking?.initials?.(m.name) || "JR"} size={30} tone={m.avatarTone || "olive"}/>
              <div style={{maxWidth:"72%"}}>
                <div style={{
                  display:"flex", alignItems:"baseline", gap:8, marginBottom:3,
                  justifyContent: mine ? "flex-end" : "flex-start",
                }}>
                  <span style={{fontSize:11, fontWeight:700, color: mine ? "var(--neon-citrus)" : "var(--cream-100)"}}>
                    {mine ? "Vos" : m.name}
                  </span>
                  <span style={{fontSize:9, color:"var(--char-500)"}}>{hora(m.createdAt)}</span>
                  {isAdmin && (
                    <button onClick={()=>del(m.id)} title="Borrar" style={{
                      border:0, background:"transparent", color:"var(--char-500)", cursor:"pointer", padding:0,
                      display:"inline-flex",
                    }}>
                      <i data-lucide="trash-2" style={{width:12, height:12}}></i>
                    </button>
                  )}
                </div>
                <div style={{
                  padding:"9px 12px", borderRadius:14,
                  background: mine ? "rgba(232,242,106,0.12)" : "var(--char-800)",
                  border:`1px solid ${mine ? "var(--neon-citrus)" : "var(--char-700)"}`,
                  color:"var(--cream-100)", fontSize:13, lineHeight:1.4,
                  wordBreak:"break-word", whiteSpace:"pre-wrap",
                }}>{m.text}</div>
              </div>
            </div>
          );
        })}
      </div>

      {/* input */}
      <div style={{
        position:"sticky", bottom:0, display:"flex", gap:8, padding:"10px 14px",
        background:"var(--char-900)", borderTop:"1px solid var(--char-700)",
      }}>
        <input
          value={text}
          onChange={(e)=>setText(e.target.value)}
          onKeyDown={onKey}
          placeholder="Escribí algo..."
          maxLength={window.ProdeChat?.MAX_LEN || 500}
          style={{
            flex:1, height:42, borderRadius:999, padding:"0 16px",
            background:"var(--char-800)", color:"var(--cream-100)",
            border:"1px solid var(--char-600)", outline:"none",
            fontFamily:"var(--font-body)", fontSize:14, boxSizing:"border-box",
          }}/>
        <Btn size="md" variant="accent" icon="send" onClick={send}>
          {sending ? "..." : ""}
        </Btn>
      </div>
    </div>
  );
}

window.Chat = Chat;
```

- [ ] **Step 2: Cargar la pantalla en `index.html`** — junto a las otras pantallas (después de `<script type="text/babel" src="screens/Groups.jsx"></script>`):

```html
<script type="text/babel" src="screens/Groups.jsx"></script>
<script type="text/babel" src="screens/Chat.jsx"></script>
```

- [ ] **Step 3: Verificar**

Run: `npx --yes esbuild screens/Chat.jsx --loader:.jsx=jsx --outfile=NUL --log-level=error && node --test`
Expected: esbuild sin errores; 71/71.

- [ ] **Step 4: Commit**

```bash
git add screens/Chat.jsx index.html
git commit -m "Chat.jsx: pantalla de chat (lista + input, auto-scroll, borrar admin)"
```

---

### Task 5: pestaña Chat en la navegación (reemplaza Grupos)

**Files:**
- Modify: `app.jsx` (`SCREENS` y `NAV_ITEMS`)

- [ ] **Step 1: `SCREENS`** — en `app.jsx`, reemplazar la entrada `groups`:

```jsx
  groups:   { label:"Grupos",       comp:"Groups",   tab:"groups"  },
```

por:

```jsx
  chat:     { label:"Chat",         comp:"Chat",     tab:"chat"    },
```

- [ ] **Step 2: `NAV_ITEMS`** — reemplazar la entrada de grupos:

```jsx
  { screen:"groups",  label:"Grupos",  icon:"users" },
```

por:

```jsx
  { screen:"chat",    label:"Chat",    icon:"message-circle" },
```

(El archivo `screens/Groups.jsx` queda en el repo y se sigue cargando en index.html, pero sin entrada de nav. Vuelve con el plan de grupos.)

- [ ] **Step 3: Verificar**

Run: `npx --yes esbuild app.jsx --loader:.jsx=jsx --outfile=NUL --log-level=error && node --test`
Expected: esbuild sin errores; 71/71.

- [ ] **Step 4: Smoke test en navegador** (lo corre el controlador con el preview `prode-static`): mockear `ProdeDB.subscribeMessages` con 2 mensajes (uno con `uid` igual al de `getUser`, otro distinto), renderizar `window.Chat`, y verificar: aparecen los 2 mensajes, el propio dice "Vos" y está alineado a la derecha, el input existe, y `validMessage` corta un texto > 500. Cero errores Babel en consola.

- [ ] **Step 5: Commit**

```bash
git add app.jsx
git commit -m "Nav: pestaña Chat reemplaza el placeholder Grupos"
```

---

### QA manual (usuario, con la regla republicada)

1. Republicar `firestore.rules` (bloque `messages` nuevo).
2. Dos cuentas en la pestaña Chat se mandan mensajes y los ven aparecer en vivo, con nombre y avatar.
3. El admin borra un mensaje → desaparece para todos.
4. Salir de la pestaña Chat → el chat se desconecta (no sigue leyendo).
5. Un jugador expulsado no puede escribir (permission-denied); sí puede leer.
6. Un mensaje de > 500 caracteres se recorta a 500.
